{
  "name": "react-blur",
  "version": "0.0.4",
  "description": "Select dropdown over text in React.",
  "main": "blur.jsx",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "react-component",
    "component",
    "text",
    "select"
  ],
  "author": "javierbyte",
  "license": "ISC",
  "browserify": {
    "transform": [
      [
        "reactify",
        {
          "es6": true
        }
      ]
    ]
  },
  "peerDependencies": {
    "react": "^0.12.x"
  }
}
